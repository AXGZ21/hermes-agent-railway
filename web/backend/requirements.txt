# FastAPI backend requirements
# These are in addition to the main pyproject.toml dependencies

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
websockets>=12.0
aiosqlite>=0.19.0

# Note: PyJWT is already in the main pyproject.toml
# Don't add it here to avoid conflicts
